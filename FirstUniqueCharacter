int firstUniqChar(char* s) {
    int count[26],i,len=strlen(s);
    for(i=0;i<26;i++){
        count[i]=0;
    }
    for(i=0;i<len;i++){
        count[s[i]-'a']++;
    }
    for(i=0;i<len;i++){
        if(count[s[i]-'a']==1){
            return i;
        }
    }
    return -1;
}
